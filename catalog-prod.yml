welcome:
  message: Catalog Application running on Prod
spring:
  profiles:
    active: prod
  cloud:
    config:
      uri: http://catalog-config
  datasource:
    username: user
    password: password
    url: jdbc:postgresql://catalog-postgres/catalog
    hikari:
      connection-timeout: 2000
      maximum-pool-size: 5
      
